#collection that is ordered and un-changable
#used to group together related data

student = ('Dhv', 24, 8.8)

#2 mmethods are -index() to return the index of a passed value, count() - count the occurance of the passed value
print(f"{student.count('Dhv')}")
print(f"{student.index('Dhv')}") 